import * as THREE from 'three';

export function generateSunTexture(width, height, time, canvas = null) {
    // Move the entire function here
}

export function generateCoronaTexture(width, height, time, canvas = null) {
    // Move the entire function here
}