import * as THREE from 'three';

export function removeCelestialBodyVisuals(bodyData) {
    // Move function here
}

export function explodeCelestialBody(bodyData, scene, vfxManager, rocketSystem) {
    // Move function here
}